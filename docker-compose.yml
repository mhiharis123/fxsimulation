version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
    volumes:
      - ./FX\ October\ PnL\ updated.xlsx:/app/FX\ October\ PnL\ updated.xlsx
    restart: unless-stopped
